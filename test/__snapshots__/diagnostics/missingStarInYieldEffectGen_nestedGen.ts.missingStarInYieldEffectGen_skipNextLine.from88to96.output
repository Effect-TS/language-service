// code fix missingStarInYieldEffectGen_skipNextLine  output for range 88 - 96
import * as Effect from "effect/Effect"

// @effect-diagnostics-next-line missingStarInYieldEffectGen:off

export const shouldNotTrigger = Effect.gen(function*() {
    async function* asyncIterable() {
        yield 1
        yield 2
        yield 3
    }

    const stream = Stream.fromAsyncIterable(asyncIterable(), identity)
    const result = yield* (Stream.runCollect(stream))
    deepStrictEqual(Array.from(result), [1, 2, 3])
})