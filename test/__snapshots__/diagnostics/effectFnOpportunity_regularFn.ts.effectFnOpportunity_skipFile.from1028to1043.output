// code fix effectFnOpportunity_skipFile  output for range 1028 - 1043
/** @effect-diagnostics effectFnOpportunity:skip-file */
import * as Effect from "effect/Effect"

// Should NOT trigger - arrow function with concise body (expression body)
export const conciseArrow = () => Effect.succeed(1)

// Should NOT trigger - arrow function with block body but 5 or fewer statements
export const fewStatements = () => {
  return Effect.succeed(1)
}

// Should NOT trigger - function expression with 5 or fewer statements
export const fewStatements2 = function<T>(value: T) {
  if (value === null) return Effect.fail("Error!")
  return Effect.succeed(value)
}

// Should NOT trigger - function declaration with 5 or fewer statements
export function fewStatements3<T>(value: T) {
  if (value === null) return Effect.fail("Error!")
  return Effect.succeed(value)
}

// Should trigger - arrow function with more than 5 statements
export const manyStatements = () => {
  const a = 1
  const b = 2
  const c = 3
  const d = 4
  const e = 5
  return Effect.succeed(a + b + c + d + e)
}

// Should trigger - function expression with more than 5 statements
export const manyStatements2 = function<T>(value: T) {
  const a = 1
  const b = 2
  const c = 3
  const d = 4
  const e = 5
  if (value === null) return Effect.fail("Error!")
  return Effect.succeed(a + b + c + d + e)
}

// Should trigger - function declaration with more than 5 statements
export function manyStatements3<T>(value: T) {
  const a = 1
  const b = 2
  const c = 3
  const d = 4
  const e = 5
  if (value === null) return Effect.fail("Error!")
  return Effect.succeed(a + b + c + d + e)
}

// Should trigger - function declaration with more than 5 statements returning Effect.gen
export function manyStatementsWithGen<T>(value: T) {
  const a = 1
  const b = 2
  const c = 3
  const d = 4
  const e = 5
  if (value === null) return Effect.fail("Error!")
  return Effect.gen(function*() {
    console.log("shouldTrigger as well!")
    return yield* Effect.succeed(a + b + c + d + e)
  })
}
