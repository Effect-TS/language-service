Schema.Struct({
  _tag: Schema.Literal("User"), // <- this is the tag
  name: Schema.String,
  age: Schema.Number
})
4:20 - 8:2 | 2 | This `Schema.Struct` includes a `_tag` field. `Schema.TaggedStruct` is the tagged-struct form for this pattern and makes the tag optional in the constructor.    effect(schemaStructWithTag)