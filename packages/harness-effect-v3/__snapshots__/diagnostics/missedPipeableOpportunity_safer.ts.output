console.log(Effect.runPromise(Effect.ignore(Effect.log("Hello"))))
12:0 - 12:66 | 0 | This nested call structure has a pipeable form. `Effect.log("Hello").pipe(...)` represents the same call sequence in pipe style and may be easier to read.    effect(missedPipeableOpportunity)

console.log(
  Effect.runPromise(
    Effect.ignore(
      Effect.succeed(console.log(Effect.runPromise(Effect.log("Hello"))))
    )
  )
)
17:0 - 23:1 | 0 | This nested call structure has a pipeable form. `Effect.succeed(console.log(Effect.runPromise(Effect.log("Hello")))).pipe(...)` represents the same call sequence in pipe style and may be easier to read.    effect(missedPipeableOpportunity)