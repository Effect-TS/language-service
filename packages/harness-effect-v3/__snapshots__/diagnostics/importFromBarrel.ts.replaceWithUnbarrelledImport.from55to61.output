// code fix replaceWithUnbarrelledImport  output for range 55 - 61
// @effect-diagnostics importFromBarrel:error
import * as Effect from "./node_modules/effect/dist/dts/Effect"
import { Predicate as P } from "effect"
import { pipe } from "effect"
import * as Schema from "effect/Schema"

export const main = {
  a: Effect.void,
  b: P.isNumber,
  c: Schema.Number
}
