Layer.effect(Cache, Effect.gen(function*() {
  yield* Effect.addFinalizer(() => Effect.void)
  return { ok: true as const }
}))
8:23 - 11:3 | 0 | Seems like you are constructing a layer with a scope in the requirements.
Consider using "scoped" instead to get rid of the scope in the requirements.    effect(scopeInLayerEffect)