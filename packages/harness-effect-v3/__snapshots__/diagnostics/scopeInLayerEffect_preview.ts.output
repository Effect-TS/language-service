Layer.effect(Cache, Effect.gen(function*() {
  yield* Effect.addFinalizer(() => Effect.void)
  return { ok: true as const }
}))
8:23 - 11:3 | 0 | This layer construction leaves `Scope` in the requirement set. The scoped API removes `Scope` from the resulting requirements.    effect(scopeInLayerEffect)