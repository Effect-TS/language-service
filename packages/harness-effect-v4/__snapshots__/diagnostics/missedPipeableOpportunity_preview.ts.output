identity(identity(Schema.decodeEffect(User)({ id: 1 })))
7:23 - 7:79 | 0 | This nested call structure has a pipeable form. `Schema.decodeEffect(User)({ id: 1 }).pipe(...)` represents the same call sequence in pipe style and may be easier to read.    effect(missedPipeableOpportunity)