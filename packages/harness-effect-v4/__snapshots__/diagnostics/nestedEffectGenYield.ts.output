Effect.gen(function*() {
    yield* Effect.succeed(1)
  })
5:9 - 7:4 | 0 | This `yield*` is applied to a nested `Effect.gen(...)` that can be inlined in the parent Effect generator context.    effect(nestedEffectGenYield)

Effect.gen(function*() {
    yield* Effect.succeed(1)
  })
15:9 - 17:4 | 0 | This `yield*` is applied to a nested `Effect.gen(...)` that can be inlined in the parent Effect generator context.    effect(nestedEffectGenYield)

Effect.gen(function*() {
    yield* Effect.succeed(1)
  })
21:9 - 23:4 | 0 | This `yield*` is applied to a nested `Effect.gen(...)` that can be inlined in the parent Effect generator context.    effect(nestedEffectGenYield)

Effect.gen(function*() {
        yield* Effect.succeed(1)
      })
29:13 - 31:8 | 0 | This `yield*` is applied to a nested `Effect.gen(...)` that can be inlined in the parent Effect generator context.    effect(nestedEffectGenYield)