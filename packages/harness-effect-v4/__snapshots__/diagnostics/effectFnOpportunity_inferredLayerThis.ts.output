log
9:17 - 9:20 | 2 | This expression can be rewritten in the reusable function form `Effect.fn("MyService.log")((what) => { ... })`.    effect(effectFnOpportunity)

log
13:8 - 13:11 | 2 | This expression can be rewritten in the reusable function form `Effect.fn("MyService.log")((what) => { ... })`.    effect(effectFnOpportunity)

log
17:17 - 17:20 | 2 | This expression can be rewritten in the reusable function form `Effect.fn("MyService.log")((what) => { ... })`.    effect(effectFnOpportunity)