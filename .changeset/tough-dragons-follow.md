---
"@effect/language-service": minor
---

Add new `schemaUnsafeTypes` diagnostic which suggest use of `Schema.JsonNumber`  over `Schema.Number` and `Schema.Date` over `Schema.Date`.
This is `"off"` by defulat.`


